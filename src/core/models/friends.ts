export type SelectedFriend = VkFriendItem;

export type VkFriendItem = {
  friendId: string;
  firstName: string;
  lastName: string;
  avatar: string;
  questionsNumber: string;
  rightsNumber: string;
};
export type FriendItem = {
  id: number;
  firstName: string;
  lastName: string;
  avatar: string;
};
